extends ../layout
block variable
  -var req = request
//- block user
//-   - var user = request.user
//-   if user
//-     if user.first_name
//-       |Welcome back #{user.first_name}
//-     else
//-       |Welcome back #{user.email}
//-   else
//-     |login
block content
  h1= title
  p Welcome to #{title} this is cool
